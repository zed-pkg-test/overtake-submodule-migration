schema_version: 1
managed_by: github-test-org-factory/1.0.0
production_org: zed-pkg
test_org: zed-pkg-test
repository: overtake-submodule-migration
kind: interop
readiness: ready
objectives:
- Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies across the supported happy-path
  states and canonical fixtures.
- Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies under retries, interruption,
  concurrency, offline operation, or partial failure.
- Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies preserves authorization,
  idempotency, integrity, observability, and actionable failure classification.
always_on_checks:
- Validate test-plan and dependency metadata.
- Validate generated fixture and workflow syntax.
- Classify missing upstreams or credentials separately from product regressions.
schedule_utc: 23 4 * * 2,5
expensive: false
required_infrastructure: []
cases:
- id: TC-001
  title: Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies across the supported
    happy-path states and canonical fixtures.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-002
  title: Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies under retries, interruption,
    concurrency, offline operation, or partial failure.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-003
  title: Verify conversion of nested .gitmodules into pinned .zpkg.toml and .zpkg.lock dependencies preserves authorization,
    idempotency, integrity, observability, and actionable failure classification.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
